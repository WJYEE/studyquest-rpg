import { TimerManager } from "../../features/timer/components/TimerManager";

export default function TimerPage() {
  return (
    <main className="mx-auto w-full max-w-lg px-4 py-8">
      <TimerManager />
    </main>
  );
}
