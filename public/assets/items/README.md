# Item icon assets (future)

No files here yet — shop/inventory UI currently uses emoji (🪙/💎) and text,
not per-item icons.

When item art is ready, name files after `Item.id` in
`src/lib/itemCatalog.ts` so they can be looked up directly:

```
items/
  straw-hat.png
  wizard-hat.png
  headband.png
  golden-crown.png
```
