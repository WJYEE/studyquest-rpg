# Background music (future)

No files here yet — v1.1 does not implement the sound system, only the
folder structure it will use.

Planned tracks, one loop per context bucket (not per-route):

```
audio/bgm/
  menu-loop.mp3     # Home, Subjects, Character, Shop, Dashboard
  focus-loop.mp3    # Timer, while a session is running
```
